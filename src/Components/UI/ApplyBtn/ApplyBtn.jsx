import React from 'react';
import css from './ApplyBtn.module.scss'

const ApplyBtn = () => {
    return (
        <div className={css.apply_btn}>
            Apply now
        </div>
    );
};

export default ApplyBtn;